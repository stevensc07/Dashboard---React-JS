import React from 'react';
import Navigation from  './navigation';

const Page = () => (
    <div>
        <h1 style={{ color: 'green'}} > Dashboard</h1>
        <Navigation/>
    </div>
);

export default Page;