
export const data = [];
for (let i = 0; i < 2; i++) {
  data.push({
    key: i,
    name: `Stevens ${i}`,
    lastName: `Correa ${i}`,
    id: 1045747348 + i,
    rol: `Administrador ${i}`,
    state: `Activo ${i}`,
    tel: 3215362088 + i,
    mail: `stevensc@uninorte.edu.co ${i}`,
  });
}

