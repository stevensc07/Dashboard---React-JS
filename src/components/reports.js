import React from 'react';
import Navigation from  './navigation';

const Page = () => (
    <div>
        <h1 style={{ color: 'red'}} > Reports</h1>
        <Navigation/>
    </div>
);

export default Page;